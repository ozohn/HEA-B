type User {
  id: ID!
  userid: String!
  username: String
  userimage: String
  userdesc: String
  works: [Work!]
  getUserInfo: User!
  getSearchedUsers: [User!]
}

type Work {
  id: ID!
  title: String!
  description: String
  photo: String
}
